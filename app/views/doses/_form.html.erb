<div class="container">
  <div class="form-inputs">
    <%= simple_form_for(dose) do |f| %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
     <%= f.submit class: "btn btn-primary" %>
  </div>
  <% end %>
</div>
